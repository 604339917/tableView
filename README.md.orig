tableView
=========

Display content dynamically from my web service into table view xcode.


This project is experimentation in parsing JSON using an external web service. We then take that JSON and display it in a dynamic table view is IOS.

A tableViewController class was created (super classed by UITableView)
&
A tableCekk (sic) class was created (supser classed by UITableCell)

Mutable arrays used.
